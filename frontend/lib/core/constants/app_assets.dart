/// Asset paths (images). Since there are no actual image files in the
/// assets directory, we use network placeholder images from picsum.photos.
class AppAssets {
  AppAssets._();

  // We'll use network images from picsum.photos for product placeholders.
  // These are referenced in dummy data.
}
